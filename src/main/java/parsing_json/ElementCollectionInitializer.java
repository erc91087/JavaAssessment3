package parsing_json;

/*
ElementCollectionInitializer

The ElementCollectionInitializer class should implement a generate method
that will load the json from the file periodic_table.json into an ElementCollection and
return that ElementCollection.
 */

import java.io.FileNotFoundException;

public class ElementCollectionInitializer {
    public static ElementCollection generate() throws FileNotFoundException {

     //   ElementCollection elementCollection = new ElementCollection();
        return null;
    }
}
